import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "git.shin.codeeditor",
  appName: "Shin Code Editor",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
